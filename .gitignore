/mdsmith
/mdsmith-release
/mdsmith-secreview
*.test
*.prof
eval/corpus/config.local.yml
coverage.out
cover.out
coverage_*.out
/.tmp/
docs/research/conciseness/spikes/wasm-embedded-inference/classifier.wasm
internal/rules/concisenessscoring/wasmclassifier/classifier.wasm
# WASM engine build outputs (cmd/mdsmith-wasm/build.sh); rebuilt on
# demand for plan 217, never committed.
cmd/mdsmith-wasm/dist/
e2e-cover/
unit.cov
merged.cov
.github/scripts/node_modules/
.claude/worktrees/
# Pulled from the orphan assets branch at site-build time by
# mdsmith-release pull-site-assets; never committed.
website/static/img/demo.gif

# PGO profiles are generated locally, never committed: a committed
# profile burdens every merge with a repo-specific binary artifact.
# See plan/ for wiring PGO into the release build instead.
cmd/mdsmith/default.pgo

# Build pass machine-local state (plan 2606101548): the incremental
# cache, per-recipe staging dirs, and the per-clone build trust marker.
.mdsmith/build-cache.json
.mdsmith/build-staging/
.mdsmith.yml.trust

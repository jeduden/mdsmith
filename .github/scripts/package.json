{
  "name": "mdsmith-ci-scripts",
  "version": "0.0.0-dev",
  "private": true,
  "description": "CI scripts run by .github/workflows/. Bun-executed TypeScript so the repo does not have to add Python as a second runtime language.",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "check-secret-rotations": "bun run check-secret-rotations.ts",
    "record-rotation": "bun run record-rotation.ts"
  },
  "dependencies": {
    "yaml": "2.6.1"
  },
  "devDependencies": {
    "@types/bun": "1.3.14"
  }
}

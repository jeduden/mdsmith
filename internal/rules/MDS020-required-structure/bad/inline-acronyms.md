---
settings:
  inline-schema:
    acronyms:
      known-safe: [API, JSON]
diagnostics:
  - line: 3
    column: 1
    message: 'acronym "OIDC" used without parenthesised expansion on first use'
---
# Notes

OIDC is configured but not expanded on first use.

The API call returns JSON.

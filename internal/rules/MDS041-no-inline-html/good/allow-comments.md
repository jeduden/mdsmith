# Document with Comments

HTML comments are allowed by default
(`allow-comments: true`).

<!-- This comment is fine -->

No diagnostics are emitted for comments unless
`allow-comments` is explicitly set to `false`.

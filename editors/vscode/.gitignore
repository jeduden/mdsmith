node_modules/
dist/
coverage/
*.vsix
# Headless VS Code integration test (test:e2e): the downloaded VS Code
# instance and the compiled mocha harness. Both are regenerated on demand.
.vscode-test/
out-e2e/
package-lock.json
yarn.lock
pnpm-lock.yaml
# build.ts copies the repo-root LICENSE here before packaging
# (vsce requires LICENSE next to package.json). The repo root is
# the single source of truth, so the staged copy is ignored.
LICENSE

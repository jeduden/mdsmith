# Generated by `mdsmith-release sync-channels` from
# docs/development/release-channels/*.md — do not edit by hand.
- title: Go
  summary: '`go install` compiles mdsmith from the tagged module source with the host Go 1.24+ toolchain; no prebuilt binary is downloaded.'
  mechanism: toolchain
  artifact: cli
  command: go install github.com/jeduden/mdsmith/cmd/mdsmith@latest
  audience: Go developers with a working Go toolchain
  platforms:
    - go
    - linux
    - macos
    - windows
  url: https://pkg.go.dev/github.com/jeduden/mdsmith
  weight: 1
- title: npm
  summary: Root `@mdsmith/cli` plus one platform-specific subpackage per supported host, all published via OIDC Trusted Publishing.
  mechanism: push
  artifact: cli
  command: npm install -g @mdsmith/cli
  audience: Node / TypeScript repos and npm-friendly CI
  platforms:
    - node
    - linux
    - macos
    - windows
  url: https://www.npmjs.com/package/@mdsmith/cli
  weight: 2
- title: npx
  summary: '`npx @mdsmith/cli` runs mdsmith from the npm package with no global install, caching the platform binary after first use.'
  mechanism: toolchain
  artifact: cli
  command: npx @mdsmith/cli check .
  audience: One-off checks without a global install
  platforms:
    - node
    - linux
    - macos
    - windows
  url: https://www.npmjs.com/package/@mdsmith/cli
  weight: 3
- title: PyPI
  summary: One platform-tagged wheel per supported host, published via OIDC Trusted Publishing.
  mechanism: push
  artifact: cli
  command: pip install mdsmith
  audience: Python projects and Python-only CI images
  platforms:
    - python
    - linux
    - macos
    - windows
  url: https://pypi.org/project/mdsmith/
  weight: 4
- title: uvx
  summary: '`uvx mdsmith` runs the PyPI wheel through uv with no persistent install, execing the bundled binary from uv''s cache.'
  mechanism: toolchain
  artifact: cli
  command: uvx mdsmith check .
  audience: Ephemeral runs via uv
  platforms:
    - python
    - linux
    - macos
    - windows
  url: https://pypi.org/project/mdsmith/
  weight: 5
- title: pipx
  summary: '`pipx install mdsmith` puts the PyPI wheel''s `mdsmith` console script on PATH inside its own isolated virtualenv.'
  mechanism: toolchain
  artifact: cli
  command: pipx install mdsmith
  audience: Isolated CLI install on Python hosts
  platforms:
    - python
    - linux
    - macos
    - windows
  url: https://pypi.org/project/mdsmith/
  weight: 6
- title: Homebrew
  summary: The `jeduden/homebrew-mdsmith` tap installs the checksum-verified prebuilt binary for macOS or Linux on Intel or arm64.
  mechanism: pull
  artifact: cli
  command: brew install jeduden/mdsmith/mdsmith
  audience: macOS and Linux via Homebrew
  platforms:
    - macos
    - linux
  url: https://github.com/jeduden/homebrew-mdsmith
  weight: 7
- title: mise
  summary: mise's `ubi` backend installs mdsmith from GitHub release assets; the short `mise use mdsmith` form awaits a registry entry.
  mechanism: pull
  artifact: cli
  command: mise use -g ubi:jeduden/mdsmith@latest
  audience: Repos using mise; works today via GitHub releases
  platforms:
    - macos
    - linux
  url: https://mise.jdx.dev/
  weight: 8
- title: asdf
  summary: The `jeduden/asdf-mdsmith` plugin installs the checksum-verified prebuilt binary; the short form awaits the asdf-plugins registry entry.
  mechanism: pull
  artifact: cli
  command: asdf plugin add mdsmith https://github.com/jeduden/asdf-mdsmith.git
  audience: Repos standardized on asdf
  platforms:
    - macos
    - linux
  url: https://github.com/jeduden/asdf-mdsmith
  weight: 9
- title: GitHub Releases
  summary: Per-platform mdsmith binaries plus the .vsix, the checksum file, and a Sigstore signature, attached to a tag-named release.
  mechanism: push
  artifact: cli
  command: curl -LO https://github.com/jeduden/mdsmith/releases/latest/download/mdsmith-<os>-<arch>
  command-windows: Invoke-WebRequest https://github.com/jeduden/mdsmith/releases/latest/download/mdsmith-windows-amd64.exe -OutFile mdsmith.exe
  audience: Air-gapped hosts and direct binary control
  platforms:
    - linux
    - macos
    - windows
  url: https://github.com/jeduden/mdsmith/releases
  weight: 10
- title: Scoop
  summary: The `jeduden/scoop-mdsmith` bucket installs the checksum-verified prebuilt binary for Windows amd64 via Scoop.
  mechanism: pull
  artifact: cli
  command: scoop install mdsmith
  audience: Windows users with Scoop installed
  platforms:
    - windows
  url: https://github.com/jeduden/scoop-mdsmith
  weight: 11
- title: Visual Studio Marketplace
  summary: The mdsmith VS Code extension `.vsix`, published via a long-lived Marketplace publisher PAT.
  mechanism: push
  artifact: vscode-extension
  command: code --install-extension jeduden.mdsmith
  audience: Stock VS Code, GitHub Codespaces, and github.dev
  platforms:
    - editor
  url: https://marketplace.visualstudio.com/items?itemName=jeduden.mdsmith
  weight: 11
- title: Open VSX
  summary: The same `.vsix` republished to Open VSX so VSCodium, Cursor, Theia, and Gitpod can install it.
  mechanism: push
  artifact: vscode-extension
  command: codium --install-extension jeduden.mdsmith
  audience: VSCodium, Cursor, Theia, and Gitpod
  platforms:
    - editor
  url: https://open-vsx.org/extension/jeduden/mdsmith
  weight: 12
- title: Flatpak
  summary: A single-file `.flatpak` bundle built in CI from the x86_64 Linux release binary and attached to each GitHub release, installed by file with host filesystem access for the linter.
  mechanism: push
  artifact: cli
  command: curl -LO https://github.com/jeduden/mdsmith/releases/latest/download/mdsmith-x86_64.flatpak && flatpak install ./mdsmith-x86_64.flatpak
  audience: Sandboxed Linux x86_64 desktops via Flatpak
  platforms:
    - linux
  url: https://github.com/jeduden/mdsmith/releases
  weight: 13
- title: Obsidian
  summary: A `mdsmith-obsidian-<version>.zip` of the plugin's five load files — the WebAssembly engine among them — attached to each GitHub release and installed by unzipping into a vault's `.obsidian/plugins/mdsmith/` folder.
  mechanism: push
  artifact: obsidian-plugin
  command: unzip mdsmith-obsidian-<version>.zip -d <vault>/.obsidian/plugins/mdsmith/
  audience: Obsidian vaults on desktop and mobile
  platforms:
    - editor
  url: https://github.com/jeduden/mdsmith/releases
  weight: 14
